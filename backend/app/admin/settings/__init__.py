"""Settings subpackage.

The settings **service** lives in ``app.admin.services.settings_service`` (kept
with the other services for consistency); this package is reserved for
settings-specific helpers such as export/import in a future stage.
"""
