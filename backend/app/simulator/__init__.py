"""Simulation & Training Platform (Stage 17).

A self-contained training contour: it lets dispatchers train, run exercises,
replay incidents and model emergencies **without touching the production
database or the live incident/resource data**. All simulation state is
in-memory and all training artefacts are stored separately.
"""
