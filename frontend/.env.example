# Base URL of the backend API. In development the Vite dev server proxies /api,
# so the default relative base works out of the box.
VITE_API_BASE_URL=/api/v1

# Target the dev-server proxy forwards /api to (backend origin).
VITE_API_PROXY_TARGET=http://localhost:8000
